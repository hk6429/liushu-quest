# 六書字例圖 v2：Lane 4

- 日期：2026-08-07
- 範圍：`data/chars.json` index 165–219，共 55 字。
- 生成方式：Codex built-in `image_gen`，每字各自生成；`c0163` 使用已核准的校準圖。
- 風格參考：`c0001`、`c0002`、`c0083`、`c0163` 四張 v2 校準圖。
- 視覺規格：暖米白宣紙、手繪墨線與飛白、朱砂／茶褐／赭金／孔雀青酒精墨暈染、古代 Q 版人物、單一故事情節。
- 內容限制：無文字、漢字、古文字字形、英數字、Logo 或浮水印；不用 pastel 資訊卡、流程模組或現代 UI。
- 原始 PNG：`/Users/naichengchen/Downloads/liushu-picture3-v2-source/lane-4/{id}.png`。
- 網站成品：`img/chars/{id}.webp`，1280×720、WebP、低於 250KB。

## 驗證結果

- 生成／核准圖：55/55。
- 原始 PNG：55/55。
- 網站 WebP：55/55。
- 尺寸：全數 1280×720，精確 16:9。
- 大小：全數低於 250KB。
- 接觸表目視：整條 lane 紙色、角色比例、墨線與酒精墨色票一致；未見 pastel 模組或可辨識文字。
- 失敗清單：無。

## 字例

`c0148` 梅、`c0149` 淺、`c0150` 清、`c0151` 悶、`c0152` 晴、`c0153` 期、`c0154` 殘、`c0155` 湖、`c0156` 菜、`c0215` 飯、`c0157` 園、`c0158` 想、`c0159` 楓、`c0160` 煎、`c0161` 賊、`c0162` 箋、`c0163` 聞、`c0216` 語、`c0164` 銅、`c0165` 閣、`c0166` 閩、`c0167` 箱、`c0168` 蝗、`c0169` 課、`c0217` 請、`c0218` 橋、`c0170` 燈、`c0171` 錢、`c0172` 錦、`c0173` 霖、`c0174` 頸、`c0175` 鴨、`c0176` 霜、`c0177` 鴿、`c0178` 雞、`c0179` 辯、`c0180` 露、`c0181` 老、`c0182` 考、`c0183` 空、`c0184` 頂、`c0185` 竅、`c0186` 顛、`c0187` 之、`c0188` 而、`c0219` 自、`c0189` 西、`c0190` 我、`c0191` 來、`c0192` 其、`c0193` 舍、`c0194` 要、`c0195` 烏、`c0196` 然、`c0220` 萬。
