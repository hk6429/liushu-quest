{
 "name": "liushu-quest",
 "private": true,
 "type": "module",
 "scripts": {
  "build:data": "node scripts/merge.mjs --update-id-map",
  "validate": "node scripts/validate.mjs",
  "content": "node scripts/content.test.mjs",
  "logic": "node scripts/logic.mjs",
  "smoke": "node scripts/smoke.mjs",
  "test": "npm run validate && npm run content && npm run logic && npm run smoke"
 },
 "devDependencies": {
  "playwright-core": "^1.62.1"
 }
}
